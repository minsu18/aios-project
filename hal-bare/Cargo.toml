[package]
name = "aios-hal-bare"
version = "0.1.0"
edition = "2021"
description = "AIOS HAL — no_std bare-metal subset for kernel integration"

[features]
default = []
llama = []  # Link libllama.a for on-device inference; requires target/llama-build/libllama.a
