[workspace]
resolver = "2"
members = [
    "kernel",
    "kernel-rpi",
    "hal",
    "ai-core",
    "boot",
    "drivers",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
