[workspace]
resolver = "2"
members = [
    "kernel",
    "kernel-rpi",
    "hal",
    "hal-bare",
    "ai-core",
    "boot",
    "drivers",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
