{
  "name": "aios-prototype",
  "version": "0.1.0",
  "description": "AIOS Phase 1 prototype: AI core + skill runtime (host validation)",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node --no-deprecation dist/index.js",
    "dev": "tsc && node --no-deprecation dist/index.js",
    "demo": "tsc && node --no-deprecation dist/index.js demo",
    "skills": "tsc && node --no-deprecation dist/index.js skills",
    "simulate": "tsc && node --no-deprecation dist/index.js simulate",
    "simulate:no-vm": "tsc && node --no-deprecation dist/index.js simulate --no-vm"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@huggingface/transformers": "^3.2.0",
    "chalk": "^5.3.0",
    "ollama": "^0.5.0",
    "openai": "^4.70.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": ["aios", "ai", "mcp", "skill"],
  "license": "MIT"
}
