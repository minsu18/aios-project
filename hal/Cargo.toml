[package]
name = "aios-hal"
version = "0.1.0"
edition = "2021"
description = "AIOS Hardware Abstraction Layer - memory, CPU, GPU, comms, camera, audio"

[dependencies]
# aios-kernel = { path = "../kernel" }  # Phase 2
